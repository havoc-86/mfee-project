import { CheckImageUrlDirective } from './checkImageUrl.directive';

describe('CheckImageUrlDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckImageUrlDirective();
    expect(directive).toBeTruthy();
  });
});
